{% extends "./../page.njk" %}

{% block Headline %}
    <h1 class="container px-4 py-8 md:px-6 xl:px-8">
        <span class="p-2 bg-gradient-to-r from-neutral-100 to-neutral-800">Explore</span>
    </h1>

    <nav class="container px-4 md:px-6 xl:px-8">
        <ul class="flex space-x-4 overflow-x-auto flex-nowrap">
            {% for i in range(0, 48) -%}
                <li>
                    <button class="p-2 mb-4 bg-neutral-900 rounded shadow text-neutral-50">tag</button>
                </li>
            {% endfor %}
        </ul>
    </nav>
{% endblock %}

{% block content %}
    <section class="grid grid-cols-1 gap-2 lg:gap-4 xl:gap-6 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 2xl:grid-cols-5 3xl:grid-cols-6">
        {% for i in range(0, 24) -%}
            <article class="h-full col-span-1 transition-all duration-200 ease-linear bg-white rounded shadow hover:bg-neutral-50 hover:shadow-lg">
                <header>
                    <img class="rounded-t shadow" src="/media/images/placeholder.png">
                    <h2 class="p-4 text-xl font-medium">Title</h2>
                </header>
                <div class="px-4 text-sm">
                    <p>Lorem ipsum dolor sit amet consectetur adipisicing elit. Nostrum vel similique explicabo provident? Eaque voluptatem ut natus facere corporis ullam expedita dolorem? Esse expedita temporibus asperiores illum dolorem necessitatibus quidem?</p>
                </div>
                <footer class="px-4">
                    <ul class="flex pt-4 pb-2 mb-2 space-x-2 overflow-x-auto overflow-y-hidden">
                        {% for i in range(0, 3) -%}
                            <li class="font-bold bg-neutral-200 rounded-full shadow">
                                <a href='#' class='p-2'>#tag</a>
                            </li>
                        {% endfor %}
                    </ul>
                </footer>
            </article>
        {%- endfor %}
    </section>
{% endblock %}